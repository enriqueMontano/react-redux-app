import React, { Fragment } from "react";
import AppBar from "../appBar";
import { CssBaseline } from "@material-ui/core";

const Page = (props) => (
  <Fragment>
    <CssBaseline />
    <AppBar />
  </Fragment>
);

export default Page;
